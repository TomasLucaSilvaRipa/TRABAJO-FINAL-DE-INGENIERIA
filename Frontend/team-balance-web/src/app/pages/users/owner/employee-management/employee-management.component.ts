import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { forkJoin } from 'rxjs';
import { Rol, RolesService } from '../../../../services/roles.service';
import { FichaEmpleado, ResourcesService, Skill } from '../../../../services/resources.service';
import { PerfilEmpleado, UsuarioGestion, UsuariosService } from '../../../../services/usuarios.service';

@Component({ selector: 'app-employee-management', imports: [ReactiveFormsModule], templateUrl: './employee-management.component.html', changeDetection: ChangeDetectionStrategy.OnPush })
export class EmployeeManagement {
  private readonly formBuilder = inject(FormBuilder); private readonly usuariosService = inject(UsuariosService); private readonly rolesService = inject(RolesService); private readonly recursosService = inject(ResourcesService);
  readonly usuarios = signal<UsuarioGestion[]>([]); readonly roles = signal<Rol[]>([]); readonly skills = signal<Skill[]>([]); readonly soporteInicialDisponible = signal(false); readonly cargando = signal(true); readonly error = signal(''); readonly mensaje = signal(''); readonly editandoId = signal<number | null>(null); readonly rolesSeleccionados = signal<number[]>([]); readonly skillsSeleccionadas = signal<number[]>([]);
  readonly form = this.formBuilder.group({ nombre: ['', Validators.required], apellido: ['', Validators.required], email: ['', [Validators.required, Validators.email]], passwordHash: [''], costoHora: [0, Validators.min(0)], horasDisponiblesSemanales: [40, Validators.min(1)], seniority: ['Junior'], estadoLaboral: ['Activo'], horaInicio: ['09:00', Validators.required], horaFin: ['18:00', Validators.required], observacionDisponibilidad: [''] });
  readonly skillForm = this.formBuilder.group({ nombre: ['', Validators.required], categoria: [''] });
  constructor() { this.cargar(); }
  cargar(): void { this.cargando.set(true); forkJoin({ usuarios: this.usuariosService.consultarUsuarios(), roles: this.rolesService.consultarRolesAsignablesAgencia(), soporte: this.usuariosService.soporteInicialDisponible(), skills: this.recursosService.consultarSkills() }).subscribe({ next: ({ usuarios, roles, soporte, skills }) => { this.usuarios.set(usuarios); this.skills.set(skills); this.soporteInicialDisponible.set(soporte.disponible); if (!soporte.disponible) { this.roles.set(roles); this.cargando.set(false); return; } this.usuariosService.consultarRolSoporteInicial().subscribe({ next: (rolSoporte) => { this.roles.set([...roles, rolSoporte]); this.cargando.set(false); }, error: () => { this.roles.set(roles); this.cargando.set(false); } }); }, error: () => { this.error.set('No fue posible cargar los usuarios, roles y skills de la agencia.'); this.cargando.set(false); } }); }
  guardar(): void {
    this.error.set(''); this.mensaje.set('');
    if (this.form.invalid || this.rolesSeleccionados().length === 0 || (this.editandoId() === null && !this.form.controls.passwordHash.value)) { this.error.set('Completá los datos obligatorios, la contraseña temporal y al menos un rol.'); return; }
    if (this.tieneTipoUsuario('Empleado') && this.skillsSeleccionadas().length === 0) { this.error.set('Seleccioná al menos una skill para el empleado.'); return; }
    const datos = this.form.getRawValue(); const usuario: Partial<UsuarioGestion> = { id: this.editandoId() ?? 0, nombre: datos.nombre ?? '', apellido: datos.apellido ?? '', email: datos.email ?? '', passwordHash: datos.passwordHash ?? '', roles: this.roles().filter(rol => this.rolesSeleccionados().includes(rol.id)), empleado: this.tieneTipoUsuario('Empleado') ? this.crearPerfilEmpleado() : null };
    if (this.editandoId() === null) { this.usuariosService.registrarUsuario(usuario).subscribe({ next: (registrado) => this.guardarFichaSiCorresponde(registrado.id, 'Usuario registrado. Se envió un correo para validar su cuenta.'), error: (respuesta: { error?: string }) => this.error.set(respuesta.error || 'No fue posible guardar el usuario.') }); return; }
    this.usuariosService.modificarUsuario(usuario as UsuarioGestion).subscribe({ next: () => this.guardarFichaSiCorresponde(this.editandoId()!, 'Usuario actualizado correctamente.'), error: (respuesta: { error?: string }) => this.error.set(respuesta.error || 'No fue posible guardar el usuario.') });
  }
  editar(usuario: UsuarioGestion): void { this.editandoId.set(usuario.id); this.rolesSeleccionados.set(usuario.roles.map(rol => rol.id)); this.skillsSeleccionadas.set([]); this.form.patchValue({ nombre: usuario.nombre, apellido: usuario.apellido, email: usuario.email, passwordHash: '', costoHora: usuario.empleado?.costoHora ?? 0, horasDisponiblesSemanales: usuario.empleado?.horasDisponiblesSemanales ?? 40, seniority: usuario.empleado?.seniority ?? 'Junior', estadoLaboral: usuario.empleado?.estadoLaboral ?? 'Activo', horaInicio: '09:00', horaFin: '18:00', observacionDisponibilidad: '' }); if (usuario.empleado) { this.recursosService.consultarFichaEmpleado(usuario.id).subscribe({ next: (ficha) => { this.skillsSeleccionadas.set(ficha.empleadoSkills.map(skill => skill.idSkill)); this.form.patchValue({ horaInicio: ficha.disponibilidadBase?.horaInicio ?? '09:00', horaFin: ficha.disponibilidadBase?.horaFin ?? '18:00', observacionDisponibilidad: ficha.disponibilidadBase?.observacion ?? '', horasDisponiblesSemanales: ficha.disponibilidadBase?.horasSemanales ?? usuario.empleado?.horasDisponiblesSemanales ?? 40 }); }, error: () => this.error.set('No fue posible cargar la ficha laboral del empleado.') }); } }
  cancelarEdicion(): void { this.editandoId.set(null); this.rolesSeleccionados.set([]); this.skillsSeleccionadas.set([]); this.form.reset({ costoHora: 0, horasDisponiblesSemanales: 40, seniority: 'Junior', estadoLaboral: 'Activo', horaInicio: '09:00', horaFin: '18:00', observacionDisponibilidad: '' }); }
  cambiarRol(idRol: number, seleccionado: boolean): void { this.rolesSeleccionados.update(roles => seleccionado ? [...new Set([...roles, idRol])] : roles.filter(id => id !== idRol)); }
  cambiarSkill(idSkill: number, seleccionado: boolean): void { this.skillsSeleccionadas.update(skills => seleccionado ? [...new Set([...skills, idSkill])] : skills.filter(id => id !== idSkill)); }
  registrarSkill(): void { if (this.skillForm.invalid) { return; } const datos = this.skillForm.getRawValue(); this.recursosService.registrarSkill({ nombre: datos.nombre ?? '', categoria: datos.categoria ?? '' }).subscribe({ next: () => { this.skillForm.reset(); this.recursosService.consultarSkills().subscribe(skills => this.skills.set(skills)); }, error: () => this.error.set('No se pudo registrar la skill.') }); }
  cambiarEstadoSkill(skill: Skill): void { this.recursosService.cambiarEstadoSkill(skill.id, !skill.activo).subscribe({ next: () => this.recursosService.consultarSkills().subscribe(skills => this.skills.set(skills)), error: () => this.error.set('No se pudo actualizar la skill.') }); }
  cambiarEstado(usuario: UsuarioGestion): void { this.usuariosService.cambiarEstado(usuario.id, !usuario.activo).subscribe({ next: () => this.cargar(), error: (respuesta: { error?: string }) => this.error.set(respuesta.error || 'No fue posible actualizar el estado.') }); }
  tieneTipoUsuario(tipoUsuario: string): boolean { return this.rolesSeleccionados().some(id => this.roles().find(rol => rol.id === id)?.tipoUsuario === tipoUsuario); }
  rolesTexto(usuario: UsuarioGestion): string { return usuario.roles.map(rol => rol.nombre).join(', '); }
  private guardarFichaSiCorresponde(idUsuario: number, mensajeExito: string): void { if (!this.tieneTipoUsuario('Empleado')) { this.finalizarGuardado(mensajeExito); return; } const ficha: FichaEmpleado = this.crearFichaEmpleado(); this.recursosService.guardarFichaEmpleado(idUsuario, ficha).subscribe({ next: () => this.finalizarGuardado(mensajeExito), error: (respuesta: { error?: string }) => this.error.set(respuesta.error || 'El usuario fue guardado, pero no se pudo completar su ficha laboral.') }); }
  private finalizarGuardado(mensajeExito: string): void { this.mensaje.set(mensajeExito); this.cancelarEdicion(); this.cargar(); }
  private crearPerfilEmpleado(): PerfilEmpleado { const datos = this.form.getRawValue(); return { costoHora: Number(datos.costoHora ?? 0), horasDisponiblesSemanales: Number(datos.horasDisponiblesSemanales ?? 40), seniority: datos.seniority ?? 'Junior', estadoLaboral: datos.estadoLaboral ?? 'Activo' }; }
  private crearFichaEmpleado(): FichaEmpleado { const datos = this.form.getRawValue(); return { empleadoSkills: this.skillsSeleccionadas().map(idSkill => ({ idSkill, nivel: 'Intermedio', activo: true })), disponibilidadBase: { horaInicio: datos.horaInicio ?? '09:00', horaFin: datos.horaFin ?? '18:00', horasSemanales: Number(datos.horasDisponiblesSemanales ?? 40), observacion: datos.observacionDisponibilidad ?? '', activo: true } }; }
}
